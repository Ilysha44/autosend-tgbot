# autosend-tgbot

In this bot you can send any repetitive message through command

`/send <amount> <message>`

1. use `pip install telebot`

2. paste your bot token in `BOT_TOKEN`

3. paste your id into `whitelist`

4. run this bot

5. enjoy!
